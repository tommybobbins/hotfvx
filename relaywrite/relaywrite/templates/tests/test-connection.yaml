apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "relaywrite.fullname" . }}-test-connection"
  labels:
    {{- include "relaywrite.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "relaywrite.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
