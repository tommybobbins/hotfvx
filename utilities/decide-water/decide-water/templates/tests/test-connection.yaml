apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "decide-water.fullname" . }}-test-connection"
  labels:
    {{- include "decide-water.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "decide-water.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
