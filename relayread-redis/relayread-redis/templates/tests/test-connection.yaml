apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "relayread-redis.fullname" . }}-test-connection"
  labels:
    {{- include "relayread-redis.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "relayread-redis.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
